#!/bin/bash

# -------------------------------
# RENDZZ WALLPAPER INSTALLER
# -------------------------------
PTERO_DIR="/var/www/pterodactyl"
TARGET_FILE="$PTERO_DIR/resources/views/templates/base/core.blade.php"
BACKUP_FILE="${TARGET_FILE}.bak_$(date -u +"%Y-%m-%d-%H-%M-%S")"

# TERIMA PARAMETER DARI TELEGRAM BOT
WALLPAPER_URL="${1:-https://files.catbox.moe/txg7nh.jpg}"
PUBLIC_WALLPAPER_DIR="$PTERO_DIR/public/rendzz"
PUBLIC_WALLPAPER_FILE="$PUBLIC_WALLPAPER_DIR/bg.jpg"

echo "🚀 Memulai update Blade + Wallpaper..."
echo "🖼️ Wallpaper URL: $WALLPAPER_URL"

# Backup
if [ -f "$TARGET_FILE" ]; then
  cp "$TARGET_FILE" "$BACKUP_FILE"
  echo "📦 Backup: $BACKUP_FILE"
fi

# Download wallpaper
echo "📥 Downloading wallpaper..."
mkdir -p "$PUBLIC_WALLPAPER_DIR"
curl -L "$WALLPAPER_URL" -o "$PUBLIC_WALLPAPER_FILE"

if [ ! -f "$PUBLIC_WALLPAPER_FILE" ]; then
    echo "❌ Gagal download wallpaper!"
    exit 1
fi

chmod 644 "$PUBLIC_WALLPAPER_FILE"
chown -R www-data:www-data "$PUBLIC_WALLPAPER_DIR" 2>/dev/null || \
chown -R nginx:nginx "$PUBLIC_WALLPAPER_DIR" 2>/dev/null || \
chown -R apache:apache "$PUBLIC_WALLPAPER_DIR" 2>/dev/null

# Inject Blade - TANPA HALLO USER & ACCESS CONTROL
cat > "$TARGET_FILE" << 'ENDOFBLADE'
@extends('templates/wrapper', [
    'css' => ['body' => 'bg-neutral-800'],
])

@section('container')
    <div id="modal-portal"></div>
    <div id="app"></div>

    <style>
      body {
        background: url('/rendzz/bg.jpg') no-repeat center center fixed !important;
        background-size: cover !important;
      }
      .content-wrapper, .main-sidebar, .card {
        background-color: rgba(255, 255, 255, 0.9) !important;
        backdrop-filter: blur(5px);
      }
      .dark-mode .content-wrapper, 
      .dark-mode .main-sidebar, 
      .dark-mode .card {
        background-color: rgba(31, 41, 55, 0.9) !important;
        backdrop-filter: blur(5px);
      }
    </style>
@endsection
ENDOFBLADE

cd "$PTERO_DIR"
php artisan view:clear
php artisan cache:clear

echo "✅ Wallpaper berhasil diinstall: $WALLPAPER_URL"
